@model ASM_WebBanNuocUong.Models.SanPham

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card border-0 shadow rounded-4 overflow-hidden border-top border-danger border-5">
                <div class="card-body p-5 text-center">
                    <div class="display-1 text-danger mb-4">
                        <i class="fas fa-exclamation-triangle"></i>
                    </div>
                    <h3 class="fw-bold text-dark">Xác nhận xóa món?</h3>
                    <p class="text-muted mb-4">Bạn có chắc chắn muốn xóa <strong>"@Model.TenSanPham"</strong> khỏi thực đơn? Thao tác này không thể hoàn tác.</p>
                    
                    <div class="bg-light p-3 rounded-3 mb-4 d-flex align-items-center">
                        <img src="@Model.HinhAnh" class="rounded shadow-sm me-3" style="width: 50px; height: 50px; object-fit: cover;">
                        <div class="text-start">
                            <div class="fw-bold">@Model.TenSanPham</div>
                            <small class="text-danger">@Model.Gia.ToString("N0") đ</small>
                        </div>
                    </div>

                    <form asp-action="Delete">
                        <input type="hidden" asp-for="MaSanPham" />
                        <button type="submit" class="btn btn-danger btn-lg w-100 rounded-pill shadow mb-3">Vâng, Xóa món này!</button>
                        <a asp-action="Index" class="btn btn-light btn-sm text-muted rounded-pill w-100 border text-decoration-none">Hủy bỏ và quay lại</a>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>